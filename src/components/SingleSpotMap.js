import React from 'react';
import styled from 'styled-components';

const SingleSpotMap = () => {
  return <Wrapper>SingleSpotMap</Wrapper>;
};

const Wrapper = styled.article``;

export default SingleSpotMap;
