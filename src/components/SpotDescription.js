import React from 'react';
import styled from 'styled-components';

const SpotDescription = () => {
  return <Wrapper>SpotDescription</Wrapper>;
};

const Wrapper = styled.div`
  height: 55%;
`;

export default SpotDescription;
